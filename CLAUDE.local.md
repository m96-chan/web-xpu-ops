# CLAUDE.local.md

このリポジトリで作業するエージェント / 開発者が必ず守る開発ルール。

[voxshot](https://github.com/m96-chan/voxshot) の規約に準拠し、カーネルライブラリ
固有の 3 つ(規約 7・8・9)を足したもの。

## 開発ルール

### 1. TDD(テスト駆動開発)を徹底する

- 実装コードより先にテストを書く(Red → Green → Refactor)。
- テストのないコードをコミットしない。
- **Red とは「新しいテストが、個別に、狙った理由で落ちること」**。落ちた件数の合計では
  判断しない。5 件追加して 3 件しか落ちないなら、**通った 2 件のほうを調べる**。
- **バグ修正では、修正を一時的に無効化して新しいテストが赤くなることを確認する。**
  戻して緑に戻ることまで見る。
- **変異が実際に当たったことを確認する。** アンカーが外れて未適用のまま
  「通過」と報告した実例がこのリポジトリで既に 2 件ある。`sed` の前後で
  md5 を比べるなど、当たったことを機械的に示してから実行する。
- **コードを消しても通るテストは、観測点が間違っている。** テスト対象の分岐を削除して
  なお緑なら、直すべきはコードではなくアサーションの取り方。
- カバレッジではここは守れない。上のいずれの失敗も、行は実行されるので数値には現れない。

### 2. 推測でコードを書かない(必ずコードリーディングをする)

- 既存の実装・型・テストを読まずに API や挙動を推測して実装しない。
- 外部ライブラリの挙動が不明な場合は、型定義・ドキュメント・実際の出力を確認してから使う。
- **GPU の挙動は特に推測しない。** `pow` の精度も `sin`/`cos` の誤差も、
  仕様書ではなく実測で決まる。数値が合わないときは、まず何が原因かを測る。
- 「たぶんこう動くはず」で PR を出さない。必ず実行して確認する。

### 3. TiDD(チケット駆動開発)を徹底する

- **ISSUE のない実装は不可**。作業前に必ず ISSUE を作成する。
- 1 つの ISSUE に対して 1 つのブランチ / 1 つの PR を対応させる。
- 作業中に判明した仕様変更・追加作業は ISSUE を更新するか、新しい ISSUE を切る。

### 4. ISSUE のライフサイクル管理を徹底する

- 着手前: ISSUE を**作成**する(背景 / スコープ / スコープ外 / 完了条件を書く)。
- 作業中: 判明した事実・方針変更を ISSUE に**更新**する。
- 完了時: PR 本文に `Closes #<issue番号>` を書き、**PR のマージによってクローズ**する。
- **マージ済みブランチに push しない。** 追加の変更は新しい PR を切る。
  マージ後のブランチへ push して、その変更がどこにも入らなかった実例がある。

### 5. コミットメッセージは英語で書く

- 件名・本文とも英語。件名は ISSUE と同じ接頭辞(`feat:` / `tune:` / `fix:` / `other:`)。
- **なぜそう変更したか**を本文に書く。何を変更したかは diff を読めば分かる。
- 既に `main` にマージ済みのコミットは書き換えない。
- ISSUE / PR 本文の言語はこのルールの対象外。

### 6. CHANGELOG を同じ PR で更新する

- 利用者から観測できる変更(op 追加 / バックエンド追加 / API 変更 / 挙動変更)を含む
  PR は、`CHANGELOG.md` の `Unreleased` に項目を追加する。
- リリース時にまとめて書き起こさない。**なぜその変更が必要だったか**が新鮮なうちに書く。
- 内部リファクタリング・テストのみ・ドキュメントのみの変更は対象外。

### 7. テストケースに困ったら PyTorch に従う

このライブラリが実装する演算には、**既に定義がある**。自分で決めない。

- 端の挙動、既定値、正規化の慣習、丸めの方向。迷ったら PyTorch の実装と
  ドキュメントを確認し、それに合わせる。合わせない場合は**なぜ違えるかを書く**。
- 特に窓関数・hop・centring・mel スケール・log の底のような**複数の妥当な慣習が
  ある箇所**は、黙って選ばない。どのライブラリに一致するかを README と
  reference のコメントに明記する。
- 「もっともらしい」実装は、利用者が五分五分で微妙に違う結果を得るということ。

### 8. 初期は正確性を先に担保する

**間違っているカーネルは、速くても価値が無い。むしろ負の価値がある。**

- reference との突合が無い状態で最適化しない。何と比べて速くなったかも、
  正しいままかも分からないため。
- タイル化・融合・ベンダ固有版は、素直な実装が reference と一致してから。
- この段階で「速いが端で間違っている」を入れると、後から見つけるのは
  ベンチマークではなく利用者になる。

規約 1 の変異確認と対になる。**先に「壊れたら赤くなる」を作り、それから速くする。**

### 9. 速度はリリース前に測って README に書く

- **主張しない。推定しない。測る。**
- 数値には**測定条件を必ず添える**。デバイス、ドライバ、バックエンド、dtype、
  入力形状。条件の無い数字は再現も比較もできない。
- 絶対値ではなく**実測ルーフラインに対する割合**で書く(README 参照)。
  WebGPU はクロックも CU 数も出さないので、理論値は計算できない。
- 測っていない項目は「未測定」と書く。空欄や省略は「速い」と読まれる。

## コマンド

```bash
npm install     # 依存インストール
npm test        # GPU 上で全 op を reference と突合(アダプタが無ければスキップ)
npm run lint    # 型チェック
```

## アーキテクチャ方針

- **reference が正しさの唯一の定義**。カーネルは reference とのみ比較し、
  他のカーネルとは比較しない。バックエンドは増えるが、正しさは 1 つ。
- reference は**最も遅く、最も素直な**実装にする。速いことではなく、
  読んで明らかに正しいことが仕事。
- 許容誤差を緩めるときは、**実測した根拠を隣に書く**。緑になるまで広げない。
- GPU バインディングはネイティブモジュールで、vitest のワーカー再利用に耐えない。
  デバイスは実行全体で 1 つ作り、最後に破棄する(`vitest.config.ts`)。
